// Loaded asynchronously by LazyMotion (see MotionProvider) so Motion's
// feature bundle stays out of the initial chunk. domMax includes layout
// animations, which the layoutId nav pills require (domAnimation does not).
export { domMax as default } from "motion/react";
